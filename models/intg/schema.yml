version: 2

sources:
  - name: raw
    schema: PUBLIC
    database: RAW
    tables:
      - name: DEMO_RAW_DATA
      - name: EMPLOYEES
        columns:
        - name: employee_id 
          description: "Unique identifier for each employee."
          tests:
           - unique    
           - not_null  
      - name: DEPARTMENTS
        columns:
        - name: department_id 
          description: "Unique identifier for each employee."
          tests:
           - unique    
           - not_null  
